name: just_sonner_example
description: "Presses every just_sonner behaviour that exists, so the package can be felt."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flash: 3.1.1
  flutter:
    sdk: flutter
  just_sonner:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
