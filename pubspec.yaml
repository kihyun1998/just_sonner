name: just_sonner
description: "Stacking toasts for Flutter desktop in the spirit of sonner: a deck that fans out on hover, toasts updated in place, promise toasts, no BuildContext."
version: 0.2.0
homepage: https://github.com/kihyun1998/just_sonner
repository: https://github.com/kihyun1998/just_sonner

environment:
  sdk: ^3.11.5
  flutter: ">=3.41.7"

platforms:
  windows:
  macos:
  linux:

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  fake_async: ^1.3.3
