# thegraph build

## What this project is

A Flutter toast package in the style of Sonner: its behaviour and look,
with an API designed the Flutter way.

## References

| Source | Informs | Reached by | Binding |
|---|---|---|---|
| emilkowalski/sonner | how it works | its source tree — raw | binding (behaviour, visuals, animation); API shape is not bound |
| Flutter SDK (Overlay, animation) | how it works | local SDK source — raw | binding |

Diverging from Sonner's behaviour or look is a bug; diverging from its
React API is expected.
